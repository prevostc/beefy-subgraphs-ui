# .graphclientrc.yml
# https://github.com/graphprotocol/graph-client/blob/main/examples/composition/.graphclientrc.yml

sources:
  - name: beefy-alm
    handler:
      graphql:
        endpoint: https://api.0xgraph.xyz/subgraphs/name/beefyfinance/alm-{context.chainName:arbitrum}
    transforms:
      - autoTypeMerging: true

documents:
  - ./src/queries/*.graphql
